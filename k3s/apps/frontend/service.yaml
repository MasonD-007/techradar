apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: nextjs-template
  labels:
    app: frontend
spec:
  type: LoadBalancer
  selector:
    app: frontend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
